import Link from "next/link";import {ArrowLeft} from "lucide-react";import {Card,CardContent} from "@/components/ui/card";import {createClient} from "@/lib/supabase/server";import {EventForm} from "../event-form";
export default async function NewEventPage(){const supabase=await createClient();const {data}=supabase?await supabase.from("demands").select("id,protocol,title").neq("status","concluida").order("created_at",{ascending:false}):{data:[]};return <div className="min-h-dvh bg-slate-50 p-5 pt-20 sm:p-8 lg:pt-8"><div className="mx-auto max-w-3xl"><Link href="/dashboard/agenda" className="mb-5 inline-flex items-center gap-2 text-sm font-semibold text-emerald-800"><ArrowLeft size={16}/>Voltar à agenda</Link><h1 className="text-3xl font-bold text-slate-950">Nova atividade</h1><p className="mb-6 mt-2 text-sm text-slate-500">Planeje eventos, coberturas e compromissos da seção.</p><Card><CardContent className="p-6"><EventForm demands={data||[]}/></CardContent></Card></div></div>}
