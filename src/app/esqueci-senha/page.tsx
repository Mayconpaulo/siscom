"use client";
import Link from "next/link";import {useState} from "react";import {Brand} from "@/components/brand";import {Button} from "@/components/ui/button";import {Input} from "@/components/ui/input";import {createClient} from "@/lib/supabase/client";
export default function ForgotPage(){const[email,setEmail]=useState("");const[message,setMessage]=useState("");const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);try{const {error}=await createClient().auth.resetPasswordForEmail(email,{redirectTo:`${location.origin}/redefinir-senha`});if(error)throw error;setMessage("Se o e-mail estiver cadastrado, você receberá um link para criar uma nova senha.")}catch(err){setMessage(err instanceof Error?err.message:"Não foi possível enviar o link.")}finally{setLoading(false)}}return <main className="grid min-h-dvh place-items-center bg-slate-50 p-6"><div className="w-full max-w-md"><Brand/><h1 className="mt-10 text-3xl font-bold">Recuperar senha</h1><p className="mb-7 mt-2 text-sm text-slate-500">Enviaremos um link seguro ao seu e-mail institucional.</p><form onSubmit={submit} className="space-y-4"><Input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="E-mail institucional" required/><Button disabled={loading} className="w-full bg-emerald-950">{loading?"Enviando...":"Enviar link de recuperação"}</Button>{message&&<p className="rounded-lg bg-emerald-50 p-3 text-sm text-emerald-800">{message}</p>}</form><Link href="/login" className="mt-6 block text-center text-sm font-semibold text-emerald-800">Voltar ao acesso</Link></div></main>}
